# AI记账助手 启动与使用说明

## 1. 环境准备

### 1.1 Python环境
- 推荐 Python 3.8 及以上
- 推荐使用虚拟环境（venv）

### 1.2 Node.js环境
- 推荐 Node.js 16 及以上
- 推荐使用 npm 8 及以上

### 1.3 MySQL数据库
- 安装并启动 MySQL 服务
- 创建数据库：`data`
- 确保账号、密码与 `AIccounting-main/api.py`、`chat.py` 配置一致（默认 root/1234）

## 2. 安装依赖

### 2.1 后端依赖
```bash
cd AIccounting-main
pip install -r requirements.txt
# 如无 requirements.txt，可手动安装：
pip install flask flask-cors mysqlclient requests pandas numpy matplotlib tqdm
```

### 2.2 前端依赖
```bash
cd App
npm install
```

## 3. Windows PowerShell 执行策略设置

**如遇到 PowerShell 执行 npm run dev 报"无法加载文件，因为在此系统上禁止运行脚本"错误，请执行：**

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

- 该命令仅对当前 PowerShell 会话生效，安全且推荐。
- 然后再运行 npm 脚本。

## 4. 启动后端服务

```bash
cd AIccounting-main
python api.py
```
- 启动后端 Flask API 服务，默认监听 5000 端口。
- 保持该窗口开启。

## 5. 启动前端服务

**新开一个 PowerShell 或 CMD 窗口，进入前端目录：**

```bash
cd App
npm run dev
```
- 启动 Vue 前端开发服务器，默认监听 8080 端口。
- 首次运行如遇 PowerShell 执行策略问题，参考第3步。

## 6. 访问与使用

- 打开浏览器访问：http://localhost:8080
- 进入"记账"页面即可与 AI 记账助手对话、记账。
- 明细、图表等页面可查看历史账目和分析。

## 7. 常见问题

- **数据库连接失败**：请检查 MySQL 是否启动、账号密码是否正确、数据库 data 是否已创建。
- **端口被占用**：如 5000 或 8080 端口被占用，可在 api.py 或前端配置中修改端口。
- **PowerShell 执行策略**：务必使用 `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` 临时放开。
- **依赖缺失**：请确保 pip/npm 依赖均已安装。

## 8. 其他说明

- 后端日志和错误会在控制台输出，便于调试。
- 前端如需打包生产环境，可使用 `npm run build`。
- 如需修改数据库配置，请同步修改 `AIccounting-main/api.py` 和 `chat.py`。

---
如有疑问请联系开发者或查阅项目文档。 